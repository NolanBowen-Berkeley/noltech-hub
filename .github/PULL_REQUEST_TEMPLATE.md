**What this changes**

**Why**

**How you tested it** — say what you actually ran, not what should work.

- [ ] `npm run build` (hub) passes
- [ ] pipeline boots: `npm run pipeline` with no errors
- [ ] No `.env`, `dist/`, or real business data in the diff
- [ ] No new dependency that fetches and parses a site's HTML
